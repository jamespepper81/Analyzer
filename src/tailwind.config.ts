// Tailwind CSS v4 uses CSS-first configuration
// Most configuration is now in globals.css using @theme
// This file is only needed for plugins

export default {
  // Plugins - tailwindcss-animate still needed for custom animations
  plugins: [require('tailwindcss-animate')],
};
